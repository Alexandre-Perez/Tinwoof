<h1>DM's 🐶 : </h1>

<% @chatrooms.each do |chatroom| %>
<div class="carde">
<%= link_to chatroom_path(chatroom), class:"supeer" do %>
<% if chatroom.interlocutor_of(current_user).photo.attached? %>
  <%= cl_image_tag chatroom.interlocutor_of(current_user).photo.key, class:"photopresent" %>
<% else %>
<p>No Photo available for now ..</p>
<p class="messagedetails"><%= chatroom.interlocutor_of(current_user).first_name %></p>
  <% end %>
<% end %>
<% end %>
</div>

<div class="pieges">
</div>
