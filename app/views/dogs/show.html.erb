<style>
body{
  background-color: #FFF5E4;
}
</style>



<div class="photocontainer">
<% if @dog.photos.length != 0 %>
<div class="elementscontain">
<%= cl_image_tag @dog.photos.first.key, :class => "photoprofile", crop: :fill %>
  <% else %>
           <p>no picture available for now ...</p>
  <% end %>

  <div class="detailscontain">
  <%= link_to chatroom_path, class: "messagelink" do %>
        <% end %>
   <p class="supertitle"><%= @dog.name %>, <%= @dog.age %> yo</p>

  <p class="details1">
    <% if @dog.gender == "female" %>
      <i class="fa-solid fa-venus"></i>
    <% else %>
      <i class="fa-solid fa-mars"></i>
    <% end %>
    | <%= @dog.race %>
    | <%= @dog.height %></p>
  </div>
  </div>
</div>
 <div class="superbidule d-flex justify-content-center my-4">
<%= link_to "Chat with #{@dog.name}", chatroom_path(@users.id), :class => "bouton-message" %>
</div>
<div class="containerd d-flex justify-content-center my-4">
<p class="dogdescription "><%= @dog.description %></p>
</div>

<div class="d-flex justify-content-around superp">
<span class="details2">
    <% if @dog.gender == "female" %>
      <strong><%= @dog.user.first_name %></strong>'s daughter
    <% else %>
      <strong>🧍🏼<%= @dog.user.first_name %></strong>'s son
    <% end %>
  </span>

  <span class="addressdog">
  <i class="fa-solid fa-location-dot"></i>  <%= @dog.address %>
  </span>
  </div>
<br>
<br>

<div class="wrapperp d-flex justify-content-center">
<div class="scrolling-wrapper">
<% @comments.each do |comment| %>
<% if @dog.id == comment.dog_id %>
<div class="card"><p class="supercomment">"<%= comment.content %>"</p></div>
<% end %>
<% end %>
</div>
</div>
<br>
<h3 class="d-flex justify-content-center my-3">Leave a review to  <em>  <%= @dog.name %> </em> </h3>

<%= simple_form_for([@dog, @comment]) do |f| %>
  <div class="form-inputs">
    <%= f.input :content, label: false, placeholder: "Say something to #{@dog.name} ...", class: "textzone" %>
    <%= f.button :submit, class: "btn w-80 d-flex justify-content-center" %>
  </div>
<% end %>

<br>
<br>


<div style="width: 100%; height: 300px; margin-bottom: 85px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
</div>
